worker_processes 1;
events { worker_connections 1024 ;}
http {
    upstream frontend {
        http:
        
   
    
    server {
        listen 80;
        listen [::]:80;
        location /dummett/ {
            proxy_pass http://127.0.0.1:8700;
        }
    }
}
